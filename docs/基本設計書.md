# AI司令塔ナレッジ - 基本設計書 (v2.0)

## 1. システム概要

### 1.1 システム名
**AI司令塔ナレッジ**（進化版：自動更新機能付き）

### 1.2 目的
開発現場で「困った！」と思った時に、AIへの最適なプロンプトを即座に発行できるナレッジベース。さらに、世界中の最新AIトレンドを自動で収集し、それを「実務で使える戦術」に変換して毎日更新する仕組み。

### 1.3 コンセプト
- **毎日自動で進化するナレッジ**: 静的なメモではなく、寝ている間に最新情報が追加される「生きたナレッジベース」
- **AIが自動で情報収集**: 情報収集から分析まで、AIエージェントが自律的に実行
- **寝ている間に資産が増える**: 朝起きたら新しい戦術が追加されている仕組み

---

## 2. システム構成

### 2.1 ファイル構成

```
ai-code/
├── app.py                  # メインアプリ（画面表示）
├── styles.py               # デザイン設定
├── data/
│   └── situations.json     # ナレッジデータ（自動更新される）
├── scripts/                # 自動化スクリプト
│   ├── collector.py        # ニュース収集スクリプト
│   └── analyst.py          # 戦術分析スクリプト
├── .github/
│   └── workflows/
│       └── daily_intel.yml # 毎日自動実行の設定
├── docs/                   # ドキュメント
│   └── 基本設計書.md
└── requirements.txt        # 必要なライブラリ一覧
```

### 2.2 使っている技術

| 役割 | 技術 | 説明 |
|------|------|------|
| 画面表示 | Streamlit | Pythonで簡単にWebアプリを作れるフレームワーク |
| デザイン | カスタムCSS | モダンでかっこいい見た目にする |
| プログラミング言語 | Python 3.10以上 | メインの開発言語 |
| ニュース収集 | Google Gemini API | 最新のAIニュースを検索・要約してくれる（Google Search Grounding機能付き） |
| 戦術分析 | Google Gemini API または OpenAI API | ニュースを「実務で使える戦術」に変換してくれる（Gemini優先・無料枠あり） |
| 自動実行 | GitHub Actions | 毎日決まった時間に自動でスクリプトを実行 |
| データ保存 | JSONファイル | ナレッジデータをファイルで管理 |

---

## 3. 機能一覧

### 3.1 システム全体の流れ

```
┌─────────────────────────────────────────────────────────────┐
│                  AI司令塔ナレッジ（自動更新版）                  │
├──────────────┬──────────────────────────────┬───────────────┤
│   ニュース収集  │         戦術分析              │    画面表示    │
│   (自動実行)  │        (自動実行)              │   (ユーザー)   │
├──────────────┼──────────────────────────────┼───────────────┤
│ ・最新AIニュース│ ・実務で使えるかどうか判断        │ ・ナレッジ検索  │
│   を探す       │ ・プロンプトを作成              │ ・変数設定     │
│ ・ノイズを除去  │ ・推奨AIモデルを選ぶ            │ ・推奨AI表示   │
│ ・ソースを記録  │ ・データを整形                  │ ・プロンプト発行│
└──────┬───────┴──────────────┬───────────────┴───────┬───────┘
       │                      │                       │
       └──────────> [ situations.json ] <────────────┘
                      (ナレッジデータベース)
```

### 3.2 各機能の詳細

#### 機能1: 自動ニュース収集（毎朝6時に実行）

| 項目 | 内容 |
|------|------|
| 機能名 | 自動ニュース収集 |
| いつ動く | 毎日朝6時（日本時間） |
| 何をする | Google Gemini API（Google Search Grounding機能付き）を使って、直近48時間の「エンジニアに役立つAIニュース」を5件集める |
| 使うモデル | `gemini-1.5-flash`（無料枠対応・高速・低コスト） |
| 結果 | `news_raw.json`というファイルに保存 |
| エラー時 | APIが失敗してもログに記録するだけで、次回も実行を続ける |

#### 機能2: 戦術分析・生成（ニュース収集の後、自動実行）

| 項目 | 内容 |
|------|------|
| 機能名 | 戦術分析・生成 |
| 何をする | 集めたニュースを見て、「これをエンジニアの実務でどう使えるか？」を考え、すぐ使えるプロンプトと推奨AIモデルを作成する |
| 使うAI | Google Gemini API（`gemini-1.5-flash`、無料枠優先）または OpenAI API（`gpt-4o`、オプション） |
| AIへの指示 | 「このニュースをエンジニアの実務課題に転用する方法を、具体的なプロンプトと推奨AIモデルを含めて提案してください」 |
| 結果 | `new_tactics.json`というファイルに保存 |

#### 機能3: キーワード検索（画面で使える）

| 項目 | 内容 |
|------|------|
| 機能名 | キーワード検索 |
| 何をする | 入力したキーワードでナレッジを絞り込む |
| 検索対象 | シチュエーション、プロンプト、タグ、タイトル |
| 検索方法 | 部分一致（大文字小文字は区別しない） |

#### 機能4: タグで絞り込み（画面で使える）

| 項目 | 内容 |
|------|------|
| 機能名 | タグで絞り込み |
| 何をする | 選んだタグに該当するナレッジだけを表示する |
| 選択方法 | 複数選択可能（OR条件：どれか1つでも該当すれば表示） |

#### 機能5: プロンプト発行（画面で使える）

| 項目 | 内容 |
|------|------|
| 機能名 | プロンプト発行 |
| 何をする | 設定した変数（使用技術、ターゲット読者など）を埋め込んだ完成形プロンプトを作成・表示する |
| 表示形式 | Markdown形式のコードブロック（コピーしてすぐ使える） |

#### 機能6: 推奨AI表示（画面で使える）

| 項目 | 内容 |
|------|------|
| 機能名 | 推奨AI表示 |
| 何をする | 各ナレッジカードに「🚀 推奨: [モデル名]」というバッジと、その選定理由を表示する |
| NEWマーク | 作成日が3日以内のデータには「🔥 NEW」アイコンを付ける |

---

## 4. データ設計

### 4.1 ナレッジデータの構造（situations.json）

```json
[
  {
    "id": "20251208_01",
    "date": "2025-12-08",
    "tags": ["テスト", "効率化"],
    "title": "長文仕様書の論理矛盾チェック",
    "problem_context": "数万文字の仕様書変更差分を目視確認するコストが高い。",
    "recommended_ai": {
      "model": "Claude 3.5 Sonnet",
      "reason": "200kトークンのコンテキストと高い論理推論能力が必要なため。",
      "badge_color": "orange"
    },
    "prompt": "あなたは法務のプロです...",
    "source_news": {
      "title": "Anthropic releases Claude 3.5",
      "url": "https://..."
    }
  }
]
```

### 4.2 データ項目の説明

| 項目名 | 型 | 必須 | 説明 |
|--------|-----|------|------|
| id | 文字列 | ✓ | 一意識別子（形式: 20251208_01 = 日付_連番） |
| date | 文字列 | ✓ | 作成日（形式: 2025-12-08） |
| tags | 配列 | ✓ | タグリスト（最低1個必要） |
| title | 文字列 | ✓ | 戦術のタイトル（最大100文字） |
| problem_context | 文字列 | ✓ | どんな問題を解決するのかの説明 |
| recommended_ai | オブジェクト | ✓ | 推奨AIの情報 |
| recommended_ai.model | 文字列 | ✓ | 推奨するAIモデル名 |
| recommended_ai.reason | 文字列 | ✓ | なぜこのモデルを推奨するのかの理由 |
| recommended_ai.badge_color | 文字列 | - | バッジの色（orange/blue/green） |
| prompt | 文字列 | ✓ | AIへのプロンプトテンプレート |
| source_news | オブジェクト | - | 元になったニュースの情報 |
| source_news.title | 文字列 | - | ニュースのタイトル |
| source_news.url | 文字列 | - | ニュースのURL |

### 4.3 画面の状態管理

| キー | 型 | 説明 |
|------|-----|------|
| `mode_{id}` | 真偽値 | プロンプト発行モードがONかOFFか |

---

## 5. 画面設計

### 5.1 ナレッジカードの見た目（拡張版）

```
┌─────────────────────────────────────────────────────────────┐
│ 🔥 NEW  [2025-12-08]                                        │
│ ▼ 長文仕様書の論理矛盾チェック                                  │
├─────────────────────────────────────────────────────────────┤
│ [テスト] [効率化]  🚀 推奨: Claude 3.5 Sonnet (最強の推論力)    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ 💡 なぜClaudeを推奨するのか？                                 │
│ GPT-4oと比較して長文読解の精度が高く、矛盾検知に特化しています。     │
│                                                             │
│ シチュエーション:                                             │
│ 数万文字の仕様書変更差分を目視確認するコストが高い...              │
│                                                             │
│ [ソース: Anthropic Blog ↗]                                  │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ ✨ プロンプト発行                                         │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 画面全体のレイアウト

```
┌─────────────────────────────────────────────────────────────┐
│ 🎯 AI司令塔ナレッジ                                           │
│ 世界中のAIトレンドを自動収集 → 実務に使える戦術に変換            │
├─────────────────────────────────────────────────────────────┤
│ [🔍 キーワード検索        ] [🏷️ タグで絞り込み ▼]             │
├─────────────────────────────────────────────────────────────┤
│ ▶ ⚙️ プロンプト変数を設定（折りたたみ）                        │
│   ├ 使用技術: [Python    ]                                  │
│   └ ターゲット: [上司     ]                                  │
├─────────────────────────────────────────────────────────────┤
│ ───────────────────────────────────────────────────────────  │
│ 📚 15 件のナレッジ（🔥 NEW: 3件）                            │
│                                                             │
│ 🔥 NEW [2025-12-08] ▼ 長文仕様書の論理矛盾チェック...          │
│ 🔥 NEW [2025-12-07] ▶ GPT-4o VisionでUIデザイン自動レビュー... │
│ [2025-12-05] ▶ クライアントから「Excel 1枚」のような...        │
│ ...                                                         │
├─────────────────────────────────────────────────────────────┤
│     AI司令塔ナレッジ - 毎日自動で進化するナレッジベース            │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. API設計

### 6.1 Google Gemini API（ニュース収集用）

#### 使用ライブラリ
```python
from google import genai
from google.genai import types
```

#### リクエストの例
```python
client = genai.Client(api_key=os.getenv("GEMINI_API_KEY"))

# Google Search Groundingを有効化
grounding_tool = types.Tool(
    google_search=types.GoogleSearch()
)

config = types.GenerateContentConfig(
    tools=[grounding_tool],
    temperature=0.7
)

response = client.models.generate_content(
    model="gemini-1.5-flash",
    contents=[prompt],
    config=config
)
```

#### レスポンスの処理
- Markdown形式のレスポンスからニュースのタイトル、URL、要約を抽出
- `news_raw.json`というファイルにJSON形式で保存する

**特徴**:
- Google Search Grounding機能により、リアルタイムのWeb検索結果を活用
- 無料枠が充実（1日15Mトークン）
- 高速で低コスト

### 6.2 Google Gemini API / OpenAI API（戦術分析用）

#### OpenAI APIの例
```python
{
  "model": "gpt-4o",
  "messages": [
    {
      "role": "system",
      "content": "あなたは戦術的AIコンサルタントです。テックニュースを実務で使えるエンジニアリングプロンプトに変換してください。"
    },
    {
      "role": "user",
      "content": f"ニュース: {news_item}\n\nエンジニアがすぐに使える戦術的プロンプトを作成してください。問題の文脈、推奨AIモデルとその理由、すぐに使えるプロンプトを含めてください。"
    }
  ],
  "temperature": 0.8,
  "response_format": {"type": "json_object"}
}
```

#### レスポンスの形式
```json
{
  "tactics": [
    {
      "title": "...",
      "problem_context": "...",
      "recommended_ai": {
        "model": "...",
        "reason": "..."
      },
      "prompt": "..."
    }
  ]
}
```

---

## 7. 自動化の仕組み

### 7.1 GitHub Actionsの設定

#### 実行スケジュール
```yaml
schedule:
  - cron: '0 21 * * *'  # 日本時間 06:00 (UTC 21:00)
```

#### ジョブの定義
```yaml
name: 毎日のインテリジェンス収集

on:
  schedule:
    - cron: '0 21 * * *'
  workflow_dispatch:  # 手動実行も可能

jobs:
  収集と分析:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Python環境のセットアップ
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: 必要なライブラリのインストール
        run: |
          pip install -r requirements.txt
      
      - name: ニュース収集を実行
        env:
          PERPLEXITY_API_KEY: ${{ secrets.PERPLEXITY_API_KEY }}
        run: |
          python scripts/collector.py
      
      - name: 戦術分析を実行
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          python scripts/analyst.py
      
      - name: データをマージしてコミット
        run: |
          python scripts/merge_tactics.py
          git config user.name "AI Staff Officer"
          git config user.email "ai-staff@tactical-intel.ai"
          git add data/situations.json
          git commit -m "毎日のインテリ更新: $(date +%Y-%m-%d)"
          git push
```

### 7.2 ニュース収集スクリプト（scripts/collector.py）

```python
"""
Gemini APIの検索機能を使って最新のAI/MLニュースを集める
"""
import json
import os
from datetime import datetime
from google import genai
from google.genai import types

def collect_news():
    """最新ニュースを集める"""
    api_key = os.getenv("GEMINI_API_KEY")
    client = genai.Client(api_key=api_key)
    
    # Google Search Groundingを有効化
    grounding_tool = types.Tool(
        google_search=types.GoogleSearch()
    )
    
    config = types.GenerateContentConfig(
        tools=[grounding_tool],
        temperature=0.7
    )
    
    response = client.models.generate_content(
        model="gemini-1.5-flash",
        contents=[prompt],
        config=config
    )
    
    # Markdown形式のレスポンスをJSONに変換
    news_data = parse_markdown_to_news_items(response.text)
    
    with open("news_raw.json", "w", encoding="utf-8") as f:
        json.dump(news_data, f, ensure_ascii=False, indent=2)

if __name__ == "__main__":
    collect_news()
```

### 7.3 戦術分析スクリプト（scripts/analyst.py）

```python
"""
集めたニュースを戦術データに変換する
"""
import json
import os
from openai import OpenAI

def analyze_and_generate_tactics():
    """ニュースを戦術に変換する"""
    client = OpenAI(api_key=os.getenv("OPENAI_API_KEY"))
    
    with open("news_raw.json", "r", encoding="utf-8") as f:
        news_items = json.load(f)
    
    tactics = []
    for news in news_items:
        response = client.chat.completions.create(
            model="gpt-4o",
            messages=[
                {
                    "role": "system",
                    "content": "あなたは戦術的AIコンサルタントです..."
                },
                {
                    "role": "user",
                    "content": f"ニュース: {json.dumps(news, ensure_ascii=False)}..."
                }
            ],
            response_format={"type": "json_object"}
        )
        
        tactic = json.loads(response.choices[0].message.content)
        tactics.append(tactic)
    
    # 日付付きIDを生成
    date_str = datetime.now().strftime("%Y%m%d")
    for i, tactic in enumerate(tactics, 1):
        tactic["id"] = f"{date_str}_{i:02d}"
        tactic["date"] = datetime.now().strftime("%Y-%m-%d")
    
    with open("new_tactics.json", "w", encoding="utf-8") as f:
        json.dump(tactics, f, ensure_ascii=False, indent=2)

if __name__ == "__main__":
    analyze_and_generate_tactics()
```

---

## 8. エラー処理

### 8.1 ニュース収集のエラー処理

| エラーの種類 | どう対処するか |
|------------|--------------|
| API接続エラー | 3回までリトライ（待ち時間を徐々に増やす）、失敗したらログに記録するだけ |
| レスポンス解析エラー | エラーログに記録、次回も実行を続ける |
| 認証エラー | GitHub Actionsに通知、実行を止める |

### 8.2 戦術分析のエラー処理

| エラーの種類 | どう対処するか |
|------------|--------------|
| LLM API失敗 | 2回までリトライ、失敗したニュースはスキップ |
| JSON形式がおかしい | エラーログに記録、該当ニュースはスキップ |
| レート制限 | 待ち時間を徐々に増やしてリトライ、最大30分待つ |

### 8.3 画面のエラー処理

| エラーの種類 | どう対処するか |
|------------|--------------|
| JSON読み込み失敗 | エラーメッセージを表示、デフォルトデータで続ける |
| 検索結果が0件 | 警告メッセージを表示 |
| プロンプト生成失敗 | エラーメッセージを表示、元のプロンプトを表示 |

---

## 9. セキュリティ

### 9.1 APIキーの管理

- GitHub Secretsに保存する
  - `PERPLEXITY_API_KEY`
  - `OPENAI_API_KEY`
  - `ANTHROPIC_API_KEY`（オプション）
- ローカル開発時は`.env`ファイルを使う（`.gitignore`に追加してGitに上げない）

### 9.2 データの検証

- JSONスキーマ検証（`jsonschema`ライブラリを使う）
- 入力のサニタイゼーション（XSS対策）
- ファイルサイズ制限（`situations.json`は最大10MB）

### 9.3 アクセス制御

- GitHub Actionsの実行は`main`ブランチのみ
- コミットは自動生成ユーザー（`AI Staff Officer`）で実行

---

## 10. パフォーマンス要件

### 10.1 レスポンス時間の目標

| 操作 | 目標値 |
|------|--------|
| ページ読み込み | 2秒以内 |
| 検索・フィルタリング | 100ミリ秒以内 |
| プロンプト生成 | 50ミリ秒以内 |

### 10.2 データ量の制限

| 項目 | 制限 |
|------|------|
| ナレッジ件数 | 最大1000件 |
| JSONファイルサイズ | 最大10MB |
| 1日の新規追加 | 最大10件 |

### 10.3 最適化の方針

- フロントエンド: クライアント側でフィルタリング（即座に結果表示）
- バックエンド: JSONファイルをキャッシュ（Streamlitの`@st.cache_data`を使う）
- データ構造: インデックスは不要（件数が少ないため）

---

## 11. テスト設計

### 11.1 単体テスト

| 対象 | テスト内容 |
|------|-----------|
| `collector.py` | APIレスポンスの解析、エラー処理 |
| `analyst.py` | JSON生成、日付ID生成 |
| `app.py` | フィルタリングロジック、プロンプト生成 |

### 11.2 統合テスト

| 対象 | テスト内容 |
|------|-----------|
| Collector → Analyst | データの流れが正しいか確認 |
| GitHub Actions | 自動実行が成功するか確認 |

### 11.3 動作確認テスト

| シナリオ | テスト内容 |
|---------|-----------|
| 検索フロー | キーワード入力 → 結果表示 → プロンプト発行 |
| フィルタリング | タグ選択 → 結果絞り込み |

---

## 12. デプロイ方法

### 12.1 ローカル開発環境のセットアップ

```bash
# リポジトリをクローン
git clone https://github.com/your-org/ai-tactical-intel.git
cd ai-tactical-intel

# 仮想環境を作成
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 必要なライブラリをインストール
pip install -r requirements.txt

# 環境変数を設定
cp .env.example .env
# .envファイルを編集してAPIキーを設定

# アプリを起動
streamlit run app.py
```

### 12.2 本番環境へのデプロイ（Streamlit Cloud推奨）

1. GitHubリポジトリにプッシュ
2. Streamlit Cloudで接続
3. Secrets設定:
   - `PERPLEXITY_API_KEY`
   - `OPENAI_API_KEY`
4. デプロイ完了

### 12.3 自動更新を有効にする

- GitHub Actionsが有効になっているか確認
- `main`ブランチへの自動コミットが許可されているか確認

---

## 13. 運用・保守

### 13.1 監視項目

| 項目 | 監視方法 |
|------|---------|
| GitHub Actionsの実行状況 | GitHub Actionsタブで確認 |
| 新規ナレッジの追加 | `data/situations.json`のコミット履歴を確認 |
| APIエラー | GitHub Actionsのログを確認 |

### 13.2 ログ管理

- Collector/AnalystのログはGitHub Actionsのログに記録
- エラー発生時はGitHub Actions通知でアラート

### 13.3 データバックアップ

- GitHubリポジトリが自動バックアップ
- 週次で`data/situations.json`のスナップショットを`backups/`に保存（オプション）

---

## 14. 今後の拡張案

| 優先度 | 機能 | 説明 |
|--------|------|------|
| 高 | データ外部化 | ナレッジデータをJSON/YAMLファイルに分離（実装済み） |
| 高 | 自動収集・分析 | GitHub Actionsによる自動化（実装予定） |
| 中 | ナレッジ追加UI | 管理者がWeb画面からナレッジを追加できる |
| 中 | お気に入り機能 | よく使うナレッジをブックマークできる |
| 中 | 利用統計 | 検索・発行の履歴を記録・可視化する |
| 低 | エクスポート | プロンプトをファイル出力できる |
| 低 | マルチ言語対応 | 英語版UIを追加する |

---

## 15. 改訂履歴

| 版 | 日付 | 内容 |
|----|------|------|
| 1.0 | 2025/12/08 | 初版作成（AI司令塔ナレッジ） |
| 2.0 | 2025/12/08 | v2.0: 自動更新機能を追加<br>- 自動ニュース収集機能<br>- 戦術分析機能<br>- 推奨AI表示機能<br>- GitHub Actions自動化 |

---

## 付録

### A. 用語集

| 用語 | 説明 |
|------|------|
| 戦術 | エンジニアがすぐに使えるAIプロンプトとその文脈 |
| Collector | ニュース収集エージェント（自動実行） |
| Analyst | 戦術分析エージェント（自動実行） |
| Dynamic Intelligence | 自動で更新されるナレッジベース |

### B. 参考資料

- [Google Gemini API Documentation](https://ai.google.dev/gemini-api/docs)
- [Google GenAI Python SDK](https://cloud.google.com/vertex-ai/generative-ai/docs/start/libraries)
- [OpenAI API Documentation](https://platform.openai.com/docs)
- [Streamlit Documentation](https://docs.streamlit.io/)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
